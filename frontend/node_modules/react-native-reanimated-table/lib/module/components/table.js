import React from 'react';
import { View } from 'react-native';
export const Table = _ref => {
  let {
    style,
    borderStyle,
    children
  } = _ref;
  const borderLeftWidth = (borderStyle === null || borderStyle === void 0 ? void 0 : borderStyle.borderWidth) ?? 0;
  const borderBottomWidth = borderLeftWidth;
  const borderColor = (borderStyle === null || borderStyle === void 0 ? void 0 : borderStyle.borderColor) ?? '#000';
  const renderChildren = () => React.Children.map(children, child => /*#__PURE__*/React.cloneElement(child, borderStyle && child.type.displayName !== 'ScrollView' ? {
    borderStyle
  } : {}));
  return /*#__PURE__*/React.createElement(View, {
    style: [style, {
      borderLeftWidth,
      borderBottomWidth,
      borderColor
    }]
  }, renderChildren());
};
export const TableWrapper = _ref2 => {
  let {
    style,
    borderStyle,
    children
  } = _ref2;
  const renderChildren = () => React.Children.map(children, child => /*#__PURE__*/React.cloneElement(child, borderStyle ? {
    borderStyle
  } : {}));
  return /*#__PURE__*/React.createElement(View, {
    style: style
  }, renderChildren());
};
//# sourceMappingURL=table.js.map